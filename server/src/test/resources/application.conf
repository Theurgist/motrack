db {

  inmem = {
    url = "jdbc:h2:mem:inmem-test"
    driver = org.h2.Driver
    connectionPool = disabled
    keepAliveConnection = true
  }

  persistent = {
    url = "jdbc:h2:mem:motrackdb-test"
    driver = org.h2.Driver
    #connectionPool = disabled
    keepAliveConnection = true
  }

}